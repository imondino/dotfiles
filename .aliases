##
# Aliases

#genkey function is defined in function files, in order to pass parameters.
alias gen-ssh-key='genkey'

#Reloads Bash
alias bash_reload='source ~/.bashrc'

#Color
alias ls='ls --color' 
alias grep='grep --color' 
